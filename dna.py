import random

class DNA:
  def __init():
    self.genes = [5]


  def reproduce(self, partner_dna):
    crossover_index = random.randint(0, len(self.genes))
    child_dna = [0] * len(self.genes)
    for i 

